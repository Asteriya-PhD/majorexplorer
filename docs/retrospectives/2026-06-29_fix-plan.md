# 修复 Plan (P0-15 + P1-25) - 2026-06-29

> **目标**: 把 6 个 agent 找出的 15 P0 + 25 P1 全部修掉
> **方法**: 探索后列出依赖图, 标注可并行 / 必须串行
> **预期总耗时**: 6-8h (并行 4-5h, 串行 6-8h)

---

## 📊 依赖图 (一图看)

```
[无依赖, 全部可同时启动]                  [依赖 B]
A1 改 business.py H1 (1 min)             C 跑 render 重生 4 篇
A2 改 _normalize_xuanke (5 min)           
A3 改 render.py:729 html.escape (3 min)   
A4 改 render.py xuanke fallback hidden   
A5 修 top_schools school→name (1 min)     
A6 修 employment_direction 字段 (2 min)   
A7 改 v4_medicine h3→独立 section (15 min) 
A8 加 WishlistStore.update() (5 min)      
A9 改 _clamp Number.isFinite (2 min)      
A10 改 recommend() 不 throw (5 min)      
A11 加 detail.js top-heart-btn handler    
A12 修 mobile render_quote 字段 (5 min)   
A13 改 mobile top_schools/alumni 不截    
A14 mobile delete sec9 deep_study         
A15 加 check_major overview_v2 兼容       

[纯 config 修改, 0 代码]                  
B1 删 sitemap 2 行 404 URL (5 min)        
B2 robots.txt 加 32 UA (10 min)           
B3 _redirects 加 404 兜底 (5 min)         
B4 deploy.sh 加 Step 8 curl 验证 (30 min) 
B5 改 deploy.sh sed 范围含 mobile/majors  
B6 sw.js CACHE_NAME bump (跑 deploy.sh)    

[依赖 A + B]  跑批量注入
D1 python3 scripts/build/inject_jsonld_v2.py      (1.5h, 1253 HTML)
D2 python3 scripts/build/inject_seo.py            (30 min, og+canonical)
D3 python3 scripts/build/render_mobile.py 9 缺失   (15 min, --slug × 9)
D4 跑 render_one.py 重生 4 business H1 (5 min)   
D5 python3 scripts/audit/update_audit_registry.py --rebuild (30s)
D6 sync_lede_to_top.py 553 lede (5 min)           
D7 fix 5 fully missing mobile slug 漂移 → 删/合    
D8 fix 21 updated_at format 批量                  
D9 fix 68 data_source 缺失                        

[依赖 D]  跑 batch audit verify
E1 smart_audit.py --dry-run 验证无新增 warning   
E2 verify_mobile.py 验证 mobile 9 全在            
E3 recommender.test.mjs 验证测试 31 fail 全修     
E4 npm playwright screenshot 抽样 5 篇 visual     

[最后]
F1 bash scripts/deploy.sh "fix(seo+content): Day 36 P0+P1 全修"
   (跑完触发 sw.js CACHE_NAME bump + 4 步验证)
F2 git push origin main
```

---

## 🌊 4 阶段流水线 (按执行顺序)

### **阶段 0: 准备 (5 min)**
- [ ] `git checkout -b fix/day36-p0-p1` 切分支 (避免污染 main)
- [ ] `git status -s` 确认 working tree clean
- [ ] 备份 `data/audit_registry.json` 副本

---

### **阶段 1: 纯代码小修 (40 min) - 全部可并行, agent 1+2+3 worktree**

**A 组 (15 个独立小改, 总 40 min, 可分 3 个 worktree 并行)**:

| # | 改什么 | 文件 | 修动 | 估时 |
|---|--------|------|------|------|
| A1 | `business.py:230` 改 `{title}` | `themes/business.py` | 1 行 | 1 min |
| A2 | `_normalize_xuanke` 加 `combo`/`item` 兼容 | `v4_styles/render.py:496` | 5 行 | 5 min |
| A3 | `render.py:729` `summary[:100]` → `html.escape(summary)[:100]` | `v4_styles/render.py` | 1 行 | 3 min |
| A4 | `render.py` xuanke fallback 改 hidden | `v4_styles/render.py` | 2 行 | 3 min |
| A5 | 修 2 JSON `school` → `name` (intelligent-transportation / postal-engineering) | 2 JSON | 各 5 行 | 5 min |
| A6 | 修 3 JSON employment `direction`/`dir` → `name` (journalism / advertising / landscape-architecture2) | 3 JSON | 各 5 行 | 5 min |
| A7 | v4_medicine 提 `<h3>` 避坑指南为独立 `<section id="pitfalls">` | `v4_medicine.py:507` | 10 行 | 15 min |
| A8 | `wishlist-store.js` 加 `update(slug, patch)` | `wishlist-store.js` | 15 行 | 5 min |
| A9 | `_clamp` 改 `Number.isFinite(n) ? n : 3` | `wishlist-store.js:53` | 1 行 | 2 min |
| A10 | `recommend()` data 缺时 `return {stats:{error:'data_incomplete'}}` 不 throw | `recommender.js:302` | 3 行 | 5 min |
| A11 | `detail.js` 加 `topHeartBtn` click handler | `public/m/js/detail.js` | 3 行 | 3 min |
| A12 | `render_mobile.py:594` 改 `q.get("current") or q.get("name")` | `render_mobile.py` | 5 行 | 5 min |
| A13 | `render_mobile.py:469` + `:589` 删 `[:8]` `[:3]` | `render_mobile.py` | 2 行 | 2 min |
| A14 | `render_mobile.py:761` sec9 = "" | `render_mobile.py` | 1 行 | 1 min |
| A15 | `check_major.py` 加 `overview_v2.*` 字段兼容 (fallback to top-level) | `scripts/audit/check_major.py` | 5 行 | 5 min |

**B 组 (6 个纯 config, 0 代码, 总 1h, 单人串行)**:

| # | 改什么 | 文件 | 修动 | 估时 |
|---|--------|------|------|------|
| B1 | 删 sitemap 2 行 404 URL (`m/majors.html` + `m/preferences.html` + PC 同) | `public/sitemap.xml` | sed | 5 min |
| B2 | `robots.txt` 加 32 UA + Disallow: / | `public/robots.txt` | 32 块 | 10 min |
| B3 | `_redirects` 末尾加 `/* /404.html 404` | `public/_redirects` | 1 行 | 5 min |
| B4 | `deploy.sh` Step 8 加 4 个 curl 验 sw.js / _headers / JSON-LD / homepage | `scripts/deploy.sh` | 30 行 | 30 min |
| B5 | `deploy.sh` 行 78 sed 范围加 `public/m/majors/*.html` | `scripts/deploy.sh` | 1 行 sed | 5 min |
| B6 | sw.js CACHE_NAME bump (跑 `deploy.sh` 自动触发) | `public/sw.js` + `m/sw.js` | 0 (自动) | 0 |

**A + B 可完全并行**: 2-3 个 worktree 分组 (A_JS / A_PC / A_data) 同时改.

---

### **阶段 2: 批量跑 (2.5h) - 必须等 A 完才能跑**

**D 组 (9 个 build 任务, 串行+并行混合)**:

| # | 命令 | 依赖 | 估时 | 风险 |
|---|------|------|------|------|
| D1 | `python3 scripts/build/inject_jsonld_v2.py` | 0 | 1.5h | 跑前先 `--dry-run` 看 1 篇验证 |
| D2 | `python3 scripts/build/inject_seo.py` | 0 | 30 min | 同上 |
| D3 | `python3 scripts/build/render_mobile.py --slug cyberspace-security` 等 9 个 | 0 | 15 min | --slug 逐个跑 |
| D4 | `python3 scripts/build/render_one.py accounting` 等 4 个 | A1 完 | 5 min | 验证 H1 改成中文 |
| D5 | `python3 scripts/audit/update_audit_registry.py --rebuild` | 0 | 30s | 4 null score 修复 |
| D6 | `python3 scripts/build/sync_lede_to_top.py` (新写 5 行) | 0 | 5 min | 553 lede null 修 |
| D7 | 修 5 fully missing mobile slug (5 个 mobile slug 漂移) | 0 | 10 min | 写 schema 兼容 |
| D8 | 修 21 updated_at 格式 (regex) | 0 | 3 min | sed 批量 |
| D9 | 修 68 data_source 缺失 (backfill 脚本) | 0 | 5 min | 默认填 "人工精编" |

**关键路径**:
- D1 + D2 + D5 + D6 + D8 + D9 可**并行** (互不影响)
- D3 + D4 必须**等 A1 + D7 完**
- D4 必须**等 A1 完** (business.py 改完才能重渲)

---

### **阶段 3: Verify (30 min) - 跑 audit + 截图**

| # | 命令 | 验证项 | 估时 |
|---|------|--------|------|
| E1 | `python3 scripts/audit/smart_audit.py --dry-run` | 无新增 L1 warning | 5s |
| E2 | `python3 scripts/audit/verify_mobile.py` | mobile 9 全在 + 11 章节通过 | 10s |
| E3 | `node public/js/recommender.test.mjs` | 31 fail → 0 fail (期望) | 30s |
| E4 | `npx playwright screenshot http://localhost:8765/accounting.html` | H1 = "会计学" (中) | 5s × 5 篇 |
| E5 | `grep -c "application/ld+json" public/*.html \| head` | 0 → 648 (期望) | 5s |
| E6 | `grep -c "og:image" public/*.html \| head` | 2 → 648 (期望) | 5s |
| E7 | `grep "< 5 所" public/*.html` | 0 期望 | 5s |
| E8 | `git diff --stat` 看变更范围, 应 < 700 HTML + 几个脚本 | - | 30s |

**如果 E3 测试仍 fail**: 修 synonymMap schema 兜底 (Stage 2 没修, 需要回 A 阶段补)

---

### **阶段 4: Deploy (5 min) - 跑 deploy.sh 闭环**

| # | 命令 | 验证 | 估时 |
|---|------|------|------|
| F1 | `bash scripts/deploy.sh "fix(seo+content): Day 36 P0+P1 全修 (15 P0 + 25 P1)"` | deploy.sh Step 4.5 sw.js 真升版 + Step 8 curl 远程 4 步 | 5 min |
| F2 | 验证线上 `https://majorexplorer.com/accounting.html` H1 = "会计学" | - | 30s |
| F3 | 验证线上 `https://majorexplorer.com/sw.js` CACHE_NAME 不是 `v0-shareDay35` | - | 10s |
| F4 | 验证线上 `https://majorexplorer.com/<slug>.html` 含 `application/ld+json` (5 篇) | - | 30s |

---

## 🧵 并行 vs 串行 决策

### **可完全并行 (3 worktree)**

| Worktree | 负责 | 估时 |
|----------|------|------|
| `wt-a-js` | A8 + A9 + A10 + A11 + A12 + A13 + A14 (JS/mobile JS) | 30 min |
| `wt-b-config` | B1 + B2 + B3 + B4 + B5 + B6 (config + deploy.sh) | 1h |
| `wt-c-pc-render` | A1 + A2 + A3 + A4 + A5 + A6 + A7 + A15 (PC + Python) | 40 min |

**主 session**: 监控 + 集成 3 worktree + 阶段 2 build 跑

### **必须串行 (1 主 session)**

| Step | 串行原因 |
|------|----------|
| D1-D9 跑批量 | 互相影响 (render_mobile.py 同时跑会冲突) |
| E1-E8 验证 | D 完才能验 |
| F1-F4 部署 | E 完才能部署 |

---

## ⚠️ 风险点 + 应对

| 风险 | 应对 |
|------|------|
| **D1 inject_jsonld_v2 跑 1253 HTML 出错** | 先 `--slug accounting` 验 1 篇, 再全量 |
| **D2 inject_seo 跟 D1 冲突 (都注 head 标签)** | D1 跑完 commit 后再 D2, 或用 idempotent fence marker |
| **D3 mobile 9 missing 渲染时数据缺 (有 manifest 没 JSON)** | 检查 9 个 slug 实际 JSON 都存在, 否则先 backfill |
| **D7 5 个 mobile missing 真实是合并到其他 slug** | 看 PC 是否有 4xx 重定向, 删 5 个 mobile 即可 |
| **A1 business.py H1 改完 4 篇重渲破坏** | 先改 1 篇验证再全量 |
| **A8 WishlistStore.update 与 setScore 重复** | 复用 `_read`/`_write` 模式, 写 5 行 |
| **D4 4 篇 business 重渲 H1 → 注入 mobile 失效** | mobile 走另一套, 不影响 |
| **E3 recommender.test 修不完** | 留 P1, 后续 polish |

---

## 📋 验收清单

**修完后 grep 全过**:

```bash
# P0
✓ grep -c "application/ld+json" public/*.html → 648 (vs 0 现状)
✓ grep -c "og:image" public/*.html → 648 (vs 2)
✓ grep -c "rel=\"canonical\"" public/*.html → 648 (vs 20)
✓ grep "Strategic Management" public/*.html → 0 (vs 4)
✓ grep "< 5 所\|< 20 所\|< 10 所" public/*.html | grep "meta name=\"description\"" → 0
✓ grep "选科数据待补充" public/*.html → 0
✓ grep -c 'meta name="description"' public/m/majors/*.html → 628 (vs 0)
✓ grep -c "discipline-breadcrumb" public/m/majors/*.html → 628 (vs 0)
✓ grep "explorer-v0-shareDay35" public/sw.js public/m/sw.js → 0
✓ node public/js/recommender.test.mjs → 57/57 pass (vs 26/57)
✓ python3 scripts/audit/verify_mobile.py → 0 error
```

**总预期耗时**: 6-8h (1-2 个 session 集中修)
**总 commit 数**: 1 主 commit (按 fix-content-p0-p1 模板) 或拆 2-3 commit (A/B/D 分阶段)

---

## 💡 决策点 (✅ 用户已拍 2026-06-29 11:50)

| # | 决策点 | 选定 | 影响 |
|---|--------|------|------|
| 1 | 分支策略 | **B**: 开 `fix/day36-p0-p1` 分支 + PR 合并 | main 不直接污染 |
| 2 | deploy 时机 | **B**: 阶段 1+2 一起 deploy (1 commit 跑所有 build + deploy.sh) | 单 commit 收口 |
| 3 | OG 覆盖范围 | **B**: PC + mobile 全覆盖 (1.5h) | mobile description 必修 |
| 4 | 并行策略 | **B**: 单主 session 串行 (1.5h, 不开 worktree) | 简单, 1h 多 |
| 5 | D7 5 fully missing mobile | **A**: 真补 5 个 mobile HTML (15 min) | 完整数据, 不删 manifest slug |
| 6 | D3 9 mobile missing 4 slug 漂移 | **B**: 改 manifest slug 对齐 mobile (5 min) | 改 4 处 manifest.json |

**重新估时 (单 session 串行)**:
- 阶段 1: 1h 40min (A 40 min + B 1h 串行)
- 阶段 2: 2.5h (D 9 任务)
- 阶段 3: 30 min (E 8 验证)
- 阶段 4: 5 min (deploy)
- **总计: 4h 45min** (1 session 集中修完)
