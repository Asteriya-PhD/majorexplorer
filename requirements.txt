# gaokao-hubei-mvp Python dependencies (Day 59 / 2026-07-02 锁定)
# 自动被 .github/workflows/synth.yml + 本地开发统一安装

# === Pinned in GH Action 原硬编码 ===
# synth_trigger.py + scf/synth/* 必需
pypinyin==0.51.0
requests==2.32.3
anthropic>=0.40

# === Used by scripts/build/render_og_cover.py 等 ===
Pillow>=10.0.0
pandas>=2.0.0

# === JSON Schema 校验 (Task 3 新增) ===
# schema/major_schema.json 用 jsonschema library 做单一真相校验
jsonschema>=4.20.0
